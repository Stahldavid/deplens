{
  "name": "deplens-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.4",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "echo 'no lint'",
    "test": "echo 'no tests'",
    "publish:core": "npm --workspace @deplens/core publish --access public",
    "publish:cli": "npm --workspace @deplens/cli publish --access public",
    "publish:mcp": "npm --workspace @deplens/mcp publish --access public",
    "publish:all": "npm --workspace @deplens/core publish --access public && npm --workspace @deplens/cli publish --access public && npm --workspace @deplens/mcp publish --access public",
    "release:patch": "npm --workspace @deplens/core version patch --no-git-tag-version && npm --workspace @deplens/cli version patch --no-git-tag-version && npm --workspace @deplens/mcp version patch --no-git-tag-version",
    "release:minor": "npm --workspace @deplens/core version minor --no-git-tag-version && npm --workspace @deplens/cli version minor --no-git-tag-version && npm --workspace @deplens/mcp version minor --no-git-tag-version",
    "release:major": "npm --workspace @deplens/core version major --no-git-tag-version && npm --workspace @deplens/cli version major --no-git-tag-version && npm --workspace @deplens/mcp version major --no-git-tag-version",
    "release:tag": "git tag v$(node -p \"require('./packages/core/package.json').version\")",
    "release:all": "npm run release:patch && npm run publish:all"
  },
  "publishConfig": {
    "access": "public"
  }
}
